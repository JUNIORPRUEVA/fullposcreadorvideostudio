{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FullPOS Video Studio API + Web (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/apps/web",
      "preLaunchTask": "dev:full"
    },
    {
      "name": "FullPOS Video Studio Web Only (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/apps/web",
      "preLaunchTask": "dev:web"
    }
  ]
}
